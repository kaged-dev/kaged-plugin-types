{
	"name": "@kaged/plugin-types",
	"description": "Pure interface package — SystemPlugin lifecycle hooks, SystemPluginContext, DaemonHooks payload types, PluginLogger, and the notification channel contract consumed by daemon and system plugins",
	"version": "0.0.2",
	"license": "AGPL-3.0",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/kaged-dev/kaged-plugin-types.git"
	},
	"type": "module",
	"main": "src/index.ts",
	"types": "src/index.ts",
	"files": [
		"src"
	],
	"publishConfig": {
		"access": "public"
	},
	"scripts": {
		"test": "bun test",
		"typecheck": "tsc --noEmit",
		"format": "biome check --write ."
	},
	"devDependencies": {
		"@biomejs/biome": "^2.4.16",
		"@types/bun": "^1.3.0",
		"typescript": "^5.8.0"
	}
}
